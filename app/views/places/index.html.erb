<% @places.each do |place| %>
  <div>
    <h1><%= place.title %></h1><br />
    <i><%= place.author %></i><br />
    <i><%= place.genre %></i><br />
    <i><%= place.classification %></i><br />
    <i><%= place.ebook %></i><br />
    <i><%= place.largeprint %></i><br />
    
  </div>

  <hr />
<% end %>